class LRUD2:

  def StartAlgorithm(self):
    print("상하좌우 알고리즘2")
    x,y = 1,1
    plan = input().split()
    size = input()

    plans = ["L", "R", "U", "D"]

    for i in plan:
      for i in range(len(plan)):
      
        continue
